name: Zizmor

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  zizmor:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Run zizmor
      uses: woodruffw/zizmor@main
      with:
        config: .zizmor.yml